import { Box } from "@chakra-ui/react";
import { Outlet } from "react-router-dom";

export const ThankYou = () => {
    return (
        <Box>
            <p>ThankYou Page</p>
        </Box>
    );
};