import { Box } from "@chakra-ui/react";
import { Outlet } from "react-router-dom";

export const Checkout = () => {
    return (
        <Box>
            <p>Checkout Page</p>
        </Box>
    );
};